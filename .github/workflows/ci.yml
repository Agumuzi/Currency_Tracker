name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  test:
    name: Build and test
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check localizations
        run: python3 scripts/check-localizations.py

      - name: Run unit tests
        run: xcodebuild test -scheme "Currency Tracker" -destination "platform=macOS" -only-testing:"Currency TrackerTests"
